//root route --> "/"
//this is the root route b/c the page.js file is in the app root folder
//we use file structure, not syntax, to exemplify the route
//! file names are IMPORTANT! The components that build our routing system must  be called page

export default function HomePage() {
  return (
    <>
      <h1>Home page</h1>
    </>
  );
}
